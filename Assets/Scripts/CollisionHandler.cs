using System.Collections;
using System.Collections.Generic;
using UnityEngine;


/**
 * 
 * Collisions between players will be sent here to ensure
 * This class resolves the issue of creating two void OnCollisionEnter() instances when two players collide
 * 
 */
public class CollisionHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
